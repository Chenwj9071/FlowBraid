# FlowBraid 开发规范

## 必读文档
每次开始任务前，先按下面顺序阅读：
1. `AGENTS.md`
2. `doc/progress.md`
3. `doc/requirements.md`
4. `doc/architecture.md`
5. `doc/tech-stack.md`

## 核心目标
- 本地优先，离线可用。
- 跨平台，优先保证 Windows 和 macOS/Linux 都能跑通。
- CLI 可直接使用，流程可恢复，可审计。
- 工作流定义、运行状态和执行日志分离。
- 预留扩展点，但首版不做过度抽象。

## 设计原则
- 显式配置优先，避免隐式约定。
- 节点独立上下文，节点目录与工程目录分离。
- 运行状态必须持久化到磁盘，不能只放内存。
- 失败必须可定位、可恢复，禁止静默吞错。
- 默认采用最小惊喜原则，行为要可预测。

## 架构原则
- 调度器与执行器解耦。
- 工作流定义与运行态分离。
- 节点间通过文件、状态箱和消息箱通信，不依赖隐式共享内存。
- 门禁、人工确认和 resume 是运行时一等状态。

## 质量要求
- 新增能力必须能被端到端验证。
- 先保证闭环可用，再扩展功能。
- 不接受“先堆功能后补测试”的开发方式。

## 开发约束
- 优先小步实现，避免大而全重构。
- 首版避免不必要的插件化、后台服务化和复杂前端。
- 任何目录结构和状态格式一旦落地，先写文档再扩展。
- 完成阶段开发或重构后必须检查关键文档是否需要更新

